# Copilot Instructions

## Project Guidelines
- Use the property name GenerateThrowIfDisposed, and when generating ThrowIfDisposed for derived disposable types, make it protected override if the base type already has the method; otherwise generate it as protected virtual.